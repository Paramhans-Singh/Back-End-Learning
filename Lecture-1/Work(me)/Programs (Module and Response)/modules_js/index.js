const operations = require('./operations')

//  we used up folder ./ to move into that file and then collected module by require keyword 
//  module are the directory that can be acessible into another file 
//  require() used to include library to the existing file

console.log(operations.add(2,3));

//  npm node package manager handels all the The name npm (Node Package Manager) stems from when npm 
// first was created as a package manager for Node.js. All npm packages are defined in files called package.json. 
// The content of package.json must be written in JSON.
